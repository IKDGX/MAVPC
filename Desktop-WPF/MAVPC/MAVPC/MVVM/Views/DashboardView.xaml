<UserControl x:Class="MAVPC.MVVM.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,30">
            <StackPanel DockPanel.Dock="Left" Margin="0,0,10,0">
                <TextBlock Text="PANEL DE GESTIÓN" FontSize="32" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Administración de Puntos de Tráfico y Sensores" FontSize="14" Foreground="Gray"/>
            </StackPanel>
            <Border DockPanel.Dock="Right" Background="#1A202C" CornerRadius="20" Padding="15,5" VerticalAlignment="Center" BorderBrush="{StaticResource NeonCyan}" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Fill="#00FF00" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding SystemStatus}" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#151922" CornerRadius="8" Padding="20" Margin="0,0,15,0" Effect="{StaticResource MaterialDesignShadowDepth1}">
                <DockPanel>
                    <materialDesign:PackIcon Kind="Cctv" Width="50" Height="50" Foreground="{StaticResource NeonCyan}" DockPanel.Dock="Left"/>
                    <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TotalCameras}" FontSize="36" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Cámaras Activas" Foreground="Gray"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="#151922" CornerRadius="8" Padding="20" Margin="0,0,15,0" Effect="{StaticResource MaterialDesignShadowDepth1}">
                <DockPanel>
                    <materialDesign:PackIcon Kind="AlertOctagon" Width="50" Height="50" Foreground="{StaticResource NeonPink}" DockPanel.Dock="Left"/>
                    <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ActiveIncidents}" FontSize="36" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Incidencias Críticas" Foreground="Gray"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Button Grid.Column="2" Command="{Binding LoadDataCommand}" Height="Auto" Background="#1000F0FF" BorderBrush="{StaticResource NeonCyan}" Margin="0,0,0,0">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Refresh" HorizontalAlignment="Center" Width="40" Height="40" Foreground="{StaticResource NeonCyan}"/>
                    <TextBlock Text="SINCRONIZAR DATOS" Margin="0,10,0,0" Foreground="{StaticResource NeonCyan}"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Listado de Dispositivos" VerticalAlignment="Center" FontSize="20" Foreground="White" FontWeight="SemiBold"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Command="{Binding OpenAddFormCommand}" Content="AÑADIR PUNTO" Margin="0,0,10,0" Background="#333" BorderBrush="#555"/>

                <Button Command="{Binding ExportPdfCommand}" Background="{StaticResource NeonPink}" BorderBrush="{x:Null}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilePdfBox" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="PDF" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3">
            <TabControl Background="Transparent" BorderThickness="0" Margin="0,10,0,0">

                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" BorderThickness="1" CornerRadius="20" Padding="20,10" Margin="0,0,15,10" Background="Transparent" BorderBrush="#333">
                                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.FontSize="14" TextElement.FontWeight="Bold"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#1A202C"/>
                                            <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Foreground" Value="Gray"/>
                    </Style>
                </TabControl.Resources>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cctv" Width="20" Height="20" Margin="0,0,8,0"/>
                            <TextBlock Text="CÁMARAS DE TRÁFICO"/>
                        </StackPanel>
                    </TabItem.Header>
                    <TabItem.Style>
                        <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource NeonCyan}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource NeonCyan}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TabItem.Style>

                    <Border Background="#151922" CornerRadius="10" Padding="10" BorderBrush="{StaticResource NeonCyan}" BorderThickness="0,1,0,0">
                        <DataGrid ItemsSource="{Binding Cameras}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  SelectionMode="Single" HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333" RowBackground="Transparent" Foreground="White">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource NeonCyan}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                                <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="VISTA" Width="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Background="Transparent" BorderThickness="0" Padding="0" Click="BtnVerCamara_Click" Cursor="Hand" Height="Auto">
                                                <Grid>
                                                    <materialDesign:PackIcon Kind="VideoOff" 
                                                                         Width="25" Height="25" 
                                                                         HorizontalAlignment="Center" 
                                                                         VerticalAlignment="Center" 
                                                                         Foreground="Gray" 
                                                                         Opacity="0.4"/>

                                                    <Border CornerRadius="4" ClipToBounds="True" Margin="2">
                                                        <Image Source="{Binding UrlImagen}" Stretch="UniformToFill" Height="40">
                                                            <Image.ToolTip>
                                                                <ToolTip Background="Transparent" BorderThickness="0">
                                                                    <Border CornerRadius="8" BorderBrush="{StaticResource NeonCyan}" BorderThickness="2" Background="#1A1A1A">
                                                                        <Grid Width="400" Height="300">
                                                                            <TextBlock Text="SIN IMAGEN" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            <Image Source="{Binding UrlImagen}" Stretch="Uniform"/>
                                                                        </Grid>
                                                                    </Border>
                                                                </ToolTip>
                                                            </Image.ToolTip>
                                                        </Image>
                                                    </Border>
                                                </Grid>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                                <DataGridTextColumn Header="NOMBRE" Binding="{Binding Nombre}" Width="2*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                                <DataGridTextColumn Header="CARRETERA" Binding="{Binding Carretera}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                                <DataGridTextColumn Header="KM" Binding="{Binding Kilometro}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                                <DataGridTemplateColumn Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Editar">
                                                    <materialDesign:PackIcon Kind="Pencil" Foreground="White"/>
                                                </Button>
                                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Eliminar">
                                                    <materialDesign:PackIcon Kind="TrashCan" Foreground="{StaticResource NeonPink}"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="WarningBox" Width="20" Height="20" Margin="0,0,8,0"/>
                            <TextBlock Text="INCIDENCIAS ACTIVAS"/>
                        </StackPanel>
                    </TabItem.Header>
                    <TabItem.Style>
                        <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource NeonPink}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource NeonPink}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TabItem.Style>

                    <Border Background="#151922" CornerRadius="10" Padding="10" BorderBrush="{StaticResource NeonPink}" BorderThickness="0,1,0,0">
                        <DataGrid ItemsSource="{Binding Incidencias}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  SelectionMode="Single" HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333" RowBackground="Transparent" Foreground="White">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource NeonPink}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                                <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTemplateColumn Width="50">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <materialDesign:PackIcon Kind="{Binding IconKind}" Foreground="{Binding StatusColor}" Width="24" Height="24" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="TIPO" Binding="{Binding Tipo}" FontWeight="Bold">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                            <Setter Property="Foreground" Value="{Binding StatusColor}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="UBICACIÓN" Binding="{Binding UbicacionCompleta}" Width="2*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                                <DataGridTextColumn Header="SENTIDO" Binding="{Binding Direccion}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                                <DataGridTextColumn Header="INICIO" Binding="{Binding FechaInicio, StringFormat=HH:mm}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>
            </TabControl>
        </Grid>

        <Grid Grid.Row="3" Background="#AA000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" Width="50" Height="50" Value="0" Foreground="{StaticResource NeonCyan}"/>
        </Grid>
    </Grid>
</UserControl>